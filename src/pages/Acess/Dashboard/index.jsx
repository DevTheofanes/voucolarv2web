
import { AcessLayout as Container } from '../index'
import { HeaderContent } from '../styles'

export const AcessDashboard = (props) => {

  return (
    <Container>
      <HeaderContent>
        <span>Painel</span>
      </HeaderContent>
      <span>0 Notificações</span>
    </Container>
  );
}

